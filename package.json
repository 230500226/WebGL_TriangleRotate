{
    "name": "webgl_test",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "serve": "browser-sync start --server --files \"**/*\" --no-open",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "browser-sync": "^3.0.2"
    }
}
